{
    "comp": {
        "button": {
            "bg": {
                "default": { "value": "{sys.color.primary}" },
                "hover": { "value": "{sys.color.primary}" }
            },
            "text": {
                "primary": { "value": "{sys.color.on-primary}" },
                "default": { "value": "{ref.color.neutral.700}" }
            },
            "border": {
                "default": { "value": "{ref.color.neutral.300}" }
            },
            "radius": { "value": "6px" }
        }
    }
}
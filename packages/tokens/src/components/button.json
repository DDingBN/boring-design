{
    "comp": {
        "button": {
            "bg": {
                "default": { "value": "{sys.color.primary}" },
                "hover": { "value": "{sys.color.primary}" }
            },
            "text": {
                "value": "{sys.color.on-primary}"
            }
        }
    }
}